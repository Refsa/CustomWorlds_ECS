using Unity.Entities;
using UnityEngine;

namespace Refsa.CustomWorld.Prototype
{
    /* static class AutomaticWorldBootstrap
    {
        [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
        static void Initialize()
        {
            DefaultWorldInitialization.Initialize("Primary World", false);
            // CustomWorldInitialization.Initialize("Primary World", false);
        }
    } */
}